# Navigation Links
#   To link a homepage section, use the URL `/#section-id` (the block's `id`).
#   The weight parameter defines the order that the links will appear in.

main:
  - name: Bio
    url: /
    weight: 10
  - name: Research
    url: /#papers
    weight: 20
  - name: CV
    url: experience/
    weight: 30
